snippet     gcd
abbr        最大公約数
    int gcd(int x, int y) {
        int r;
        if(x < y) swap(x, y);

        while(y > 0){
            r = x % y;
            x = y;
            y = r;
        }
        return x;
    }
